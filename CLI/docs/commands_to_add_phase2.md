stratml --version
stratml --help
stratml doctor              # check environment, dependencies
stratml status              # system status (services, agents)
stratml config              # show current config
stratml config:set <key> <value>
stratml config:get <key>
stratml config:reset