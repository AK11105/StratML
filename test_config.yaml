constraints:
  max_cpu: null
  max_memory: null
dataset:
  path: data/iris.csv
  target_column: species
execution:
  max_iterations: 10
  random_seed: 42
  timeout_per_run: 300
logging:
  enable_mlflow: false
  enable_tensorboard: false
  log_level: info
mode: beginner
