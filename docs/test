bash
# All 46 existing unit tests (decision engine) — should all pass
pytest tests/unit/test_decision_engine.py -v

# Full unit suite
pytest tests/unit/ -v

# Smoke tests
pytest tests/smoke/ -v

# Integration tests
pytest tests/integration/ -v

# Everything
pytest -v


To also test the LLM paths (Phases 1–3), set your key first:

bash
# Test with LLM paths active (requires OpenAI key)
OPENAI_API_KEY=sk-... pytest tests/unit/test_decision_engine.py -v

# LangSmith tracing alongside
OPENAI_API_KEY=sk-... LANGCHAIN_TRACING_V2=true LANGCHAIN_API_KEY=ls-... pytest tests/unit/test_decision_engine.py -v