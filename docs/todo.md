# To-Do

1. Initial Data Profile handling for task selection and initial model decision
2. Decide exhaustive set of actions allowed in the system
3. Output structure and folder refined
4. Use single source of truth for schemas (stratml/core/schemas.py)
5. Better PDFs
6. Agent integration in decisio side (post StateObject)
7. model.py needs to be optional (config/cli), model.pkl always
8. Verify generated objects (all types) are consistent w.r.t to docs/schemas.md